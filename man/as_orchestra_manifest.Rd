% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{as_orchestra_manifest}
\alias{as_orchestra_manifest}
\title{Emit a gpfield result as an orchestra manifest}
\usage{
as_orchestra_manifest(x, ...)
}
\arguments{
\item{x}{A \code{gpfield_prediction} object.}

\item{...}{Method-specific arguments. The \code{gpfield_prediction} method accepts
\code{run_id}, an optional character run identifier; when omitted a content hash
is derived, so two identical predictions get the same identifier.}
}
\value{
An \code{orchestra_manifest} S7 object.
}
\description{
The federation's emit / adapt generic. The \code{gpfield_prediction} method turns a
smoothed surface into the shared \code{orchestra_manifest} contract:
\code{inferential_target = "predictions"}, the predictive mean and standard
deviation (and, for block support, the block bounds) carried in \code{outputs}, and
the model provenance -- kernel, estimated range, support, log-likelihood -- in
\code{metadata}. The payload is integrity-hashed so a downstream consumer can
verify it with \code{\link[=verify_manifest]{verify_manifest()}}.
}
\examples{
set.seed(1L)
grid <- expand.grid(x = seq_len(8L), y = seq_len(8L))
grid$yield <- 2 + 0.3 * grid$x + stats::rnorm(nrow(grid), 0, 0.2)
fit <- gp_fit(gpfield_spec(c("x", "y"), "yield"), grid, seed = 1L)
pred <- gp_predict(fit, grid)
as_orchestra_manifest(pred)

}
\seealso{
\code{\link[=verify_manifest]{verify_manifest()}}, \code{\link[=gp_predict]{gp_predict()}}
}
