% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{gp_fit}
\alias{gp_fit}
\title{Fit a gpfield Gaussian process to field or trial data}
\usage{
gp_fit(spec, data, seed = NULL)
}
\arguments{
\item{spec}{A \code{gpfield_spec} from \code{\link[=gpfield_spec]{gpfield_spec()}}.}

\item{data}{A \code{data.frame} carrying the coordinate columns, the optional time
column, and the response column named by \code{spec}.}

\item{seed}{Optional integer RNG seed recorded with the fit for provenance.
The exact fit is deterministic, so the seed is metadata rather than a source
of randomness.}
}
\value{
A \code{gpfield_fit} S7 object, or a \code{\link[=gpfield_abstention]{gpfield_abstention()}} with reason
\code{"degenerate_fit"} when the marginal-likelihood optimisation does not yield
a usable covariance.
}
\description{
Solves an exact Gaussian-process regression of the response on the spatial
(and optionally temporal) coordinates named by the \code{gpfield_spec}. The
coordinates and response are standardised, the chosen kernel's hyperparameters
are estimated by maximising the marginal likelihood, and the solved Cholesky
system is stored so that \code{\link[=gp_predict]{gp_predict()}} can predict cheaply -- at the observed
support or another.
}
\details{
The estimated length-scale is reported in raw coordinate units as the
effective (practical) correlation \code{range} -- the distance at which the
covariance has decayed to \code{0.05}, so points beyond it are effectively
independent. That range is the quantity the change-of-support and abstention
machinery reasons about: a prediction finer than the range is unsupported, and
the fit makes that range explicit rather than implicit.
}
\examples{
set.seed(1L)
grid <- expand.grid(x = seq_len(8L), y = seq_len(8L))
grid$yield <- 2 + 0.3 * grid$x + sin(grid$y / 2) + stats::rnorm(nrow(grid), 0, 0.2)
spec <- gpfield_spec(coords = c("x", "y"), response = "yield")
fit <- gp_fit(spec, grid, seed = 1L)
fit

}
\seealso{
\code{\link[=gpfield_spec]{gpfield_spec()}}, \code{\link[=gp_predict]{gp_predict()}}, \code{\link[=gp_field_smooth]{gp_field_smooth()}}
}
