% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field.R
\name{gp_field_smooth}
\alias{gp_field_smooth}
\title{Smooth a row-column field or trial layout}
\usage{
gp_field_smooth(
  data,
  response,
  row = "row",
  col = "col",
  kernel = "matern",
  nu = 1.5,
  refine = 1L,
  seed = NULL
)
}
\arguments{
\item{data}{A \code{data.frame} with the row, column and response columns.}

\item{response}{Character scalar naming the trait column to smooth.}

\item{row, col}{Character scalars naming the row and column coordinate columns
(default \code{"row"} and \code{"col"}).}

\item{kernel}{Character covariance kernel passed to \code{\link[=gpfield_spec]{gpfield_spec()}}:
\code{"matern"} (default) or \code{"se"}.}

\item{nu}{Matern smoothness (default \code{1.5}); ignored for \code{"se"}.}

\item{refine}{Positive integer grid-refinement factor. \code{1} (default) predicts
on the observed plot grid; \code{2} predicts on a grid twice as fine on each
axis, and so on. A refinement the field cannot support triggers an
abstention.}

\item{seed}{Optional integer seed recorded with the fit.}
}
\value{
A list with the \code{fit} (a \code{gpfield_fit}) and the \code{prediction} (a
\code{gpfield_prediction}), or a \code{\link[=gpfield_abstention]{gpfield_abstention()}} when the fit or the
prediction cannot be supported.
}
\description{
One call from a multi-environment-trial (or any row-column field) data frame
to a smoothed surface. Fits a gpfield Gaussian process over the row and column
coordinates and predicts the latent trait surface back onto a plot grid -- the
observed grid by default, or a finer grid through \code{refine}. The honest
abstention of \code{\link[=gp_predict]{gp_predict()}} is inherited: a refinement finer than the
estimated correlation range returns a \code{\link[=gpfield_abstention]{gpfield_abstention()}} rather than an
over-confident surface.
}
\examples{
set.seed(1L)
field <- expand.grid(row = seq_len(10L), col = seq_len(10L))
field$yield <- 3 + 0.2 * field$row - 0.1 * field$col +
  stats::rnorm(nrow(field), 0, 0.3)
sm <- gp_field_smooth(field, response = "yield", seed = 1L)
sm$prediction

}
\seealso{
\code{\link[=gp_fit]{gp_fit()}}, \code{\link[=gp_predict]{gp_predict()}}, \code{\link[=gp_field_plot]{gp_field_plot()}}
}
