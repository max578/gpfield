% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abstention.R
\name{gpfield_abstention}
\alias{gpfield_abstention}
\title{Construct a typed gpfield abstention}
\usage{
gpfield_abstention(
  reason,
  detail = NA_character_,
  scope = "gp_predict",
  diagnostics = list()
)
}
\arguments{
\item{reason}{Character scalar reason code. One of \code{"range_too_short"} (the
estimated correlation range is shorter than the requested prediction
spacing, so interpolation would be unsupported extrapolation between
effectively independent points), \code{"support_gap"} (a requested block has too
few -- or no -- observations within its own footprint or one correlation
range of it), \code{"degenerate_fit"} (the marginal-likelihood optimisation did
not yield a usable hyperparameter set), or \code{"empty_query"} (the prediction
grid contained no rows).}

\item{detail}{Character scalar human-readable detail, or \code{NA}.}

\item{scope}{Character scalar naming what was refused (default \code{"gp_predict"}).}

\item{diagnostics}{Optional named list of the numbers behind the refusal (for
example the estimated range and the requested spacing) so the caller can act
on the margin, not just the verdict.}
}
\value{
An object of class \code{"gpfield_abstention"}: a list with \code{reason},
\code{detail}, \code{scope}, \code{diagnostics} and \code{abstained = TRUE}.
}
\description{
Returns a small classed object signalling that a prediction could not be
honestly produced, with a machine-readable reason code and human-readable
detail. Used in place of a confident-but-wrong prediction so that callers can
branch on a refusal rather than trust an unsupported surface.
}
\examples{
a <- gpfield_abstention(
  "range_too_short",
  "estimated range 2.1 m < requested spacing 10 m"
)
is_gpfield_abstention(a)

}
