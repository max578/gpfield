% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{gp_predict}
\alias{gp_predict}
\title{Predict a gpfield surface, optionally at a different support}
\usage{
gp_predict(
  fit,
  newdata = NULL,
  support = c("point", "block"),
  blocks = NULL,
  min_support_ranges = 1,
  spacing_tol = 1,
  seed = NULL
)
}
\arguments{
\item{fit}{A \code{gpfield_fit} from \code{\link[=gp_fit]{gp_fit()}}.}

\item{newdata}{For point support, a \code{data.frame} of query coordinates with the
fit's coordinate (and time) columns. Ignored when \code{support = "block"}.}

\item{support}{Character: \code{"point"} (default) or \code{"block"}. Block support
requires \code{blocks}.}

\item{blocks}{A \code{gpfield_block_support} from \code{\link[=block_support]{block_support()}}, required when
\code{support = "block"}.}

\item{min_support_ranges}{Numeric: a block must have at least one training
observation within this many correlation ranges of its footprint, else the
verb abstains. Default \code{1}.}

\item{spacing_tol}{Numeric: the point grid's spacing may be at most this
multiple of the estimated range before the verb abstains with
\code{"range_too_short"}. Default \code{1} (spacing may not exceed the range).}

\item{seed}{Optional integer seed for the block quadrature draws
(reproducible change-of-support).}
}
\value{
A \code{gpfield_prediction} S7 object on success, or a
\code{\link[=gpfield_abstention]{gpfield_abstention()}} when the support cannot bear the request.
}
\description{
The package's central verb. Given a fitted GP, predicts the latent field at
the requested locations and, critically, at the requested support. With
\code{support = "point"} (default) the result is the GP posterior mean and standard
deviation at each query point. With \code{support = "block"} the result is the
change-of-support prediction: each block's value is the integral of the latent
field over the block, and its predictive standard deviation is computed from
the full within-block posterior covariance so spatial correlation is
propagated, not averaged away.
}
\details{
Honest abstention runs before any value is returned. If the estimated
correlation range is shorter than the requested point spacing -- so the query
grid asks the GP to interpolate between effectively independent observations --
the verb returns a \code{\link[=gpfield_abstention]{gpfield_abstention()}} with reason \code{"range_too_short"}. If
a requested block has no training observation within one correlation range of
its footprint, the verb abstains with reason \code{"support_gap"}. An empty query
abstains with \code{"empty_query"}. The rule errs toward refusing a confident wrong
surface over emitting one.
}
\examples{
set.seed(1L)
grid <- expand.grid(x = seq_len(10L), y = seq_len(10L))
grid$yield <- 2 + 0.25 * grid$x + 0.1 * grid$y +
  stats::rnorm(nrow(grid), 0, 0.2)
fit <- gp_fit(gpfield_spec(c("x", "y"), "yield"), grid, seed = 1L)

# Point prediction on a fine grid.
q <- expand.grid(x = seq(1, 10, by = 1), y = seq(1, 10, by = 1))
pt <- gp_predict(fit, q)

# Change of support: two paddock-scale block averages.
bl <- block_support(lower = rbind(c(1, 1), c(6, 6)),
                    upper = rbind(c(5, 5), c(10, 10)))
blk <- gp_predict(fit, support = "block", blocks = bl, seed = 1L)
blk

}
\seealso{
\code{\link[=gp_fit]{gp_fit()}}, \code{\link[=block_support]{block_support()}}, \code{\link[=gp_field_smooth]{gp_field_smooth()}}
}
